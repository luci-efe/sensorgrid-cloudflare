VITE_USE_MOCK=false
VITE_WORKER_URL=https://sensorgrid-ingest.lfernando-rramos.workers.dev
# Same-origin auth — proxied via Pages Functions (/auth/* → Worker)
# Leave blank or set to /auth to use same-origin (fixes mobile Safari)
VITE_AUTH_URL=/auth
